{
  "name": "yan-portf-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "clean": "turbo run clean",
    "check-types": "turbo run check-types",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{js,ts,tsx,md}\"",
    "release:fix": "./scripts/release.sh fix",
    "release:feat": "./scripts/release.sh feat",
    "release:release": "./scripts/release.sh release"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.5",
    "@trivago/prettier-plugin-sort-imports": "^6.0.2",
    "prettier": "^3.2.5",
    "prettier-plugin-organize-imports": "^4.3.0",
    "turbo": "^2.0.4",
    "vitest": "^4.1.8"
  },
  "packageManager": "bun@1.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {}
}
