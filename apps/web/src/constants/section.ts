// Anchor ids for the single-page sections. Used by the header nav, scroll spy,
// footer quick links, and in-page CTA links.
export enum SectionId {
  HERO = "hero",
  INTRODUCTION = "introduction",
  SERVICES = "services",
  PROJECTS = "projects",
  TESTIMONIALS = "testimonials",
  CONTACT = "contact",
}
