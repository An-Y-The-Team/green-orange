// Nhân sự — feature-scoped constants. Mirrors crm-api-nest's worker.module.ts;
// the server is the only thing that enforces the cap, this is here so the flag
// label and the Đang làm card describe it with the same number.
export const MAX_SHIFT_HOURS = 16;
