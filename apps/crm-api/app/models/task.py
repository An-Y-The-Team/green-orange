"""Task model — STUDENT EXERCISE (skeleton).

Fill this in using app/models/client.py as the template, then uncomment the
Task import in app/models/__init__.py. Match the crm-web `Task` type:
title, due_date (date), status, priority, assignee.
"""

# Implement TaskBase / Task(table=True) / TaskCreate / TaskPublic / TaskUpdate
# here, following app/models/client.py.
