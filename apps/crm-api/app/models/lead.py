"""Lead model — STUDENT EXERCISE (skeleton).

Fill this in using app/models/client.py as the template, then uncomment the
Lead import in app/models/__init__.py. Match the crm-web `Lead` type:
name, company, source, status, value (int), owner.
"""

# Implement LeadBase / Lead(table=True) / LeadCreate / LeadPublic / LeadUpdate
# here, following app/models/client.py.
