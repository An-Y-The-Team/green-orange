"""Deal model — STUDENT EXERCISE (skeleton).

Fill this in using app/models/customer.py as the template, then uncomment the
Deal import in app/models/__init__.py. Match the crm-web `Deal` type:
title, company, stage, amount (int), close_date (date).
"""

# Implement DealBase / Deal(table=True) / DealCreate / DealPublic / DealUpdate
# here, following app/models/customer.py.
