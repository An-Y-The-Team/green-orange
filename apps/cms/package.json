{
  "name": "@yan/cms",
  "version": "1.0.0",
  "description": "Directus config-as-code (schema snapshot + SDK setup/seed scripts). Directus itself runs from the official image — this package builds nothing.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build-schema": "bun scripts/build-schema.ts",
    "setup-access": "bun scripts/setup-access.ts",
    "seed": "bun seed/seed.ts",
    "migrate-from-payload": "bun scripts/migrate-from-payload.ts",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "5.7.2"
  }
}
